{
  "name": "nft-data-indexer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nft-data-indexer",
    "create-local": "graph create --node http://localhost:8020/ nft-data-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ nft-data-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nft-data-indexer",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.54.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
